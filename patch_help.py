import re
with open("internal/commands/commands.go", "r") as f:
    c = f.read()

target = """.مشرفين القروبات (لاستخراج أرقام أقل عدد مشرفين يغطون كل قروباتك)
.تنظيف القروبات (لحذف رسائل كل القروبات يدوياً - البوت يحذفها تلقائياً كل ساعتين)"""
new_target = """.مشرفين القروبات (لاستخراج أرقام أقل عدد مشرفين يغطون كل قروباتك)
.تنظيف القروبات (لحذف رسائل كل القروبات يدوياً - البوت يحذفها تلقائياً كل ساعتين)
.روليت (لعبة عجلة الحظ للمشرفين المؤقتين)
.اعرف [الرقم] (لكشف اسم صاحب الرقم من قاعدة بيانات Truecaller السري)"""
c = c.replace(target, new_target)

with open("internal/commands/commands.go", "w") as f:
    f.write(c)

